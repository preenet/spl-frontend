server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: serve static assets directly (customize as needed)
    location /static/ {
        expires 1y;
        add_header Cache-Control "public";
    }
}
